# Ignore generated Codex review reports (prevent stale report issues)
reports/post-push/codex-review-*.md

# Keep the directory structure and other files
!reports/post-push/.gitkeep

# Session-specific runtime state (completion promises, session tracking)
completion-state/promises/
completion-state/sessions/
completion-state/default/
completion-state/history/

# Keep directory structure
!completion-state/.gitkeep
!completion-state/promises/.gitkeep
!completion-state/sessions/.gitkeep
!completion-state/history/.gitkeep

# MCP configuration with API keys (users should copy from .mcp.json.example)
.mcp.json

# Python cache
__pycache__/
*.py[cod]
*$py.class
*.pyc
.pytest_cache/
.coverage
htmlcov/

# Node cache
node_modules/
.npm
package-lock.json

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log

# Compiled binaries
tools/tmux-nav/tmux-nav

# Serena IDE state
.serena/

# ZeroRepo analysis output (whole dir ignored)
.zerorepo/

# CoBuilder RepoMap — baseline JSON files are large machine data; keep config + manifests
.repomap/baselines/*/baseline.json
.repomap/baselines/*/baseline.prev.json
# Keep directory structure
!.repomap/baselines/.gitkeep
!.repomap/manifests/.gitkeep

# Worktree checkouts
trees/

# Root-level completion state
completion-state/

# Local settings override
settings.local.json

# Empty test artifact
test

# PEM files (private keys, certificates, etc.)
*.pem

# Temporary files
*.tmp
*.temp
tmp/
temp/

# Claire config directory
.claire/

# CoBuilder runtime state (pipelines, signals, checkpoints, runner-state)
.cobuilder/*
!.cobuilder/examples/
